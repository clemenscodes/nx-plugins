import { getDependenciesOfProject } from './getDependenciesOfProject';

describe('getDependenciesOfProject', () => {
    test('should get dependencies of project', () => {
        expect(getDependenciesOfProject).toBeDefined();
    });
});
