import type { Graph } from '../../../../models/types';
import type { ProjectGraphDependencyWithFile } from '@nx/devkit';
import { buildGraphFromDeps } from './buildGraphFromDeps';

describe('buildGraphFromDeps', () => {
    it.todo('should build graph from deps');
});
