import { getProjectConfiguration } from './getProjectConfiguration';

describe('getProjectConfiguration', () => {
    test('should get project configuration', () => {
        expect(getProjectConfiguration).toBeDefined();
    });
});
