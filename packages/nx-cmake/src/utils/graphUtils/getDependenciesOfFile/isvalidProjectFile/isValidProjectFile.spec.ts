import type { CTag } from '../../../../models/types';

describe('isValidProjectFile', () => {
    it.todo('should return true if valid project file');
    it.todo('should return false if not valid project file');
});
