{
    "$schema": "http://json-schema.org/schema",
    "version": 2,
    "title": "Debug executor",
    "description": "Debug a C or C++ binary using gdb",
    "type": "object",
    "properties": {
        "args": {
            "description": "Optional arguments which will be forwarded to gdb",
            "type": "array"
        },
        "release": {
            "description": "Whether to build in release mode",
            "type": "boolean",
            "default": false
        }
    },
    "outputCapture": "direct-nodejs",
    "required": []
}
