{
    "$schema": "http://json-schema.org/schema",
    "version": 2,
    "title": "Format executor",
    "description": "Format a C or C++ project with clang-format",
    "type": "object",
    "properties": {
        "args": {
            "description": "Optional arguments which will be forwarded to clang-format",
            "type": "array"
        },
        "verbose": {
            "description": "Verbose output",
            "default": true,
            "type": "boolean"
        },
        "editFilesInPlace": {
            "description": "Whether to edit files in place",
            "default": true,
            "type": "boolean"
        }
    },
    "outputCapture": "direct-nodejs",
    "required": []
}
