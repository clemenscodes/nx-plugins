{
    "$schema": "http://json-schema.org/schema",
    "version": 2,
    "title": "Build executor",
    "description": "Build a C or C++ library with Make",
    "type": "object",
    "properties": {
        "args": {
            "description": "Optional arguments which will be forwarded to make",
            "type": "array"
        }
    },
    "outputCapture": "direct-nodejs",
    "required": []
}
