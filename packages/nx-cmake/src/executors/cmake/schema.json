{
    "$schema": "http://json-schema.org/schema",
    "version": 2,
    "title": "CMake executor",
    "description": "Build a C or C++ project with CMake",
    "type": "object",
    "properties": {
        "args": {
            "description": "Optional arguments which will be forwarded to cmake",
            "type": "array"
        },
        "release": {
            "type": "boolean",
            "default": false,
            "description": "Whether to build in release mode"
        }
    },
    "outputCapture": "direct-nodejs",
    "required": []
}
