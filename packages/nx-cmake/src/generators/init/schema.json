{
    "$schema": "http://json-schema.org/schema",
    "$id": "Init",
    "title": "Intialize CMake support",
    "description": "Generate the boilerplate for CMake, C and C++",
    "type": "object",
    "properties": {
        "appsDir": {
            "type": "string",
            "description": "Where the binaries will be generated (nx.json has higher precedence)",
            "default": "bin",
            "aliases": ["apps", "a"],
            "$default": {
                "$source": "argv",
                "index": 0
            },
            "x-priority": "important",
            "x-prompt": "Where do you want to generate the binaries?"
        },
        "libsDir": {
            "type": "string",
            "description": "Where the libraries will be generated (nx.json has higher precedence)",
            "default": "libs",
            "aliases": ["libs", "l"],
            "$default": {
                "$source": "argv",
                "index": 1
            },
            "x-priority": "important",
            "x-prompt": "Where do you want to generate the libraries?"
        },
        "projectNameAndRootFormat": {
            "type": "string",
            "description": "Whether to derive the project location or take it as provided (nx.json has higher precendence)",
            "default": "derived",
            "aliases": ["p"],
            "enum": ["as-provided", "derived"],
            "$default": {
                "$source": "argv",
                "index": 2
            },
            "x-priority": "important",
            "x-prompt": "Derive project locations or take as provided?"
        },
        "cmakeConfigDir": {
            "type": "string",
            "description": "Where the configuration for CMake will be generated",
            "default": "cmake",
            "$default": {
                "$source": "argv",
                "index": 3
            },
            "x-priority": "important",
            "x-prompt": "Where do you want to generate the CMake configuration?"
        },
        "addClangPreset": {
            "description": "Generate a clang preset",
            "type": "boolean",
            "default": true
        },
        "skipFormat": {
            "description": "Skip formatting files.",
            "type": "boolean",
            "default": false
        }
    },
    "required": []
}
