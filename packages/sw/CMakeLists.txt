include("../../CMakeLists.txt")

cmake_minimum_required(VERSION ${CMAKE_MINIMUM_REQUIRED_VERSION})
set_project_settings(libsw ${CMAKE_CURRENT_SOURCE_DIR})
project(libsw CXX)
set_library_settings(libsw ${CMAKE_CURRENT_SOURCE_DIR})
