{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "version": 2,
    "title": "Test executor",
    "description": "Test a C library using CMocka or C++ library using googletest",
    "type": "object",
    "properties": {
        "args": {
            "type": "array",
            "description": "Optional arguments which will be forwarded to ctest"
        },
        "release": {
            "description": "Whether to build in release mode",
            "type": "boolean",
            "default": false
        },
        "outputOnFailure": {
            "description": "Whether to print output on test failure",
            "type": "boolean",
            "default": true
        }
    },
    "outputCapture": "direct-nodejs",
    "required": []
}
