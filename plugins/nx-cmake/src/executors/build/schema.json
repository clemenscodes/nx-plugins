{
    "$schema": "http://json-schema.org/schema",
    "version": 2,
    "title": "Build executor",
    "description": "Build a C or C++ project with Make",
    "type": "object",
    "properties": {
        "args": {
            "description": "Optional arguments which will be forwarded to make",
            "type": "array"
        },
        "release": {
            "description": "Whether to build in release mode",
            "type": "boolean",
            "default": false
        }
    },
    "outputCapture": "direct-nodejs",
    "required": []
}
