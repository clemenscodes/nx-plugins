{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "version": 2,
    "title": "Execute executor",
    "description": "Execute a C or C++ binary",
    "type": "object",
    "properties": {
        "args": {
            "description": "Optional arguments which will be forwarded to the executed binary",
            "type": "array"
        },
        "release": {
            "description": "Whether to build in release mode",
            "type": "boolean",
            "default": false
        }
    },
    "outputCapture": "direct-nodejs",
    "required": []
}
