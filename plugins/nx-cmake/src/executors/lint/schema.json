{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "version": 2,
    "title": "Lint executor",
    "description": "Lint a C or C++ project with clang-tidy",
    "type": "object",
    "properties": {
        "args": {
            "description": "Optional arguments which will be forwarded to clang-tidy",
            "type": "array"
        },
        "release": {
            "description": "Whether to lint in release mode",
            "type": "boolean",
            "default": false
        }
    },
    "outputCapture": "direct-nodejs",
    "required": []
}
